name: 'ekoru-services-prod'

services:
  ekoru-services:
    image: ekoru-services:prod
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    container_name: ekoru-services
    env_file:
      - .env.prod
    ports:
      - '4104:4004'
    networks:
      - ekoru-network
    deploy:
      resources:
        limits:
          cpus: '2.0' # Maximum 2 CPU cores (production environment)
          memory: 1G # Maximum 1GB RAM (production environment)
        reservations:
          cpus: '0.5' # Minimum 0.5 CPU cores reserved
          memory: 512M # Minimum 512MB RAM reserved
    healthcheck:
      test:
        [
          'CMD',
          'wget',
          '--quiet',
          '--tries=1',
          '--spider',
          'http://localhost:4104/health',
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  ekoru-network:
    external: true
